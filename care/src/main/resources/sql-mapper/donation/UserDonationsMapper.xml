<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.animal.api.donation.dao.UserDonationsMapper">

<select id="getAllDonations" parameterType="Map" resultType="com.animal.api.donation.model.response.AllDonationListResponseDTO">
SELECT
	D.IDX,
	D.NAME,
	D.SPONSOR,
	D.COMPLETION_RATE,
	D.COMPLETION_AMOUNT,
	DS.NAME AS 'STATUS'
FROM
	DONATIONS D
JOIN DONATION_STATUS DS ON
	D.IDX = DS.IDX
ORDER BY D.IDX DESC
LIMIT ${listSize} OFFSET ${cp} 

</select>


</mapper>